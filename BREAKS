IRC Client Daemon BREAKS
========================

This is the list of changes that breaks compatibility.

Between 1.0 and 1.1
-------------------

* The functions server:whois() and server:names() do not use deferred calls
  anymore.
