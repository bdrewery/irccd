---
name: mkdir
category: fs
since: 1.1
---

Create a directory recursively. This function acts like `mkdir -p`.

# Synopsis

````lua
ret, err = function fs.mkdir(path, mode)
````

# Arguments

* path, the path
* mode, the optional octal mode

# Returns

* ret, true on success or false plus an error
* err, the error message
